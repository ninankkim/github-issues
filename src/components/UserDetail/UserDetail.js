import React from 'react';
import './UserDetail.js';

export default function UserDetail(props) {
    const { login, avatar_url, bio, company, location, blog} = props.user;
    return (
        <div className="UserDetail">
            
        </div>
    )

}